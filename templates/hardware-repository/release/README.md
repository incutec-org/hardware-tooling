# Release configuration

`approved-violations.json` starts empty. Add an ERC or DRC finding only after a
human has reviewed the actual report and approved the finding for release.
Counts are maximums, not targets: fewer findings pass, while a new type or a
higher count blocks release preparation.
